name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: mydb
          MYSQL_USER: test
          MYSQL_PASSWORD: test
        ports:
          - "3306:3306"
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Update Composer
        run: composer self-update

      - name: Cache Composer Dependencies
        uses: actions/cache@v3
        with:
          path: ~/.composer/cache/files
          key: composer-${{ runner.os }}-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            composer-${{ runner.os }}-

      - name: Setup PHP & Composer
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.2
          extensions: pdo, pdo_mysql
          tools: composer

      - name: Debug Composer
        run: |
          cd backend/lumen-api
          composer diagnose

      - name: Install Lumen dependencies
        run: |
          cd backend/lumen-api
          composer install --no-interaction --prefer-dist --no-scripts
          cp .env.example .env
          php artisan cache:clear
          php artisan config:clear

      - name: Vérification du fichier .env
        run: |
          cd backend/lumen-api
          cat .env

      - name: Run Composer Post Install Scripts
        run: |
          cd backend/lumen-api
          composer run-script post-install-cmd || echo "No post-install scripts"

      - name: Wait for MySQL
        run: |
          echo "Waiting for MySQL to be ready..."
          until mysqladmin ping -h"127.0.0.1" -uroot -proot --silent; do
            sleep 2
          done
          echo "MySQL is up!"

      - name: Run Lumen migrations
        run: |
          cd backend/lumen-api
          php artisan migrate --env=testing --force

      - name: Run Lumen tests
        run: |
          cd backend/lumen-api
          vendor/bin/phpunit --testdox
